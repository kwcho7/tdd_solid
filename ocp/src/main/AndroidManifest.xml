<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rsupport.example.ocp" >

    <application
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:name=".ApplicationOCP"
        >
        <activity android:name=".activities.web.WebViewActivity" />
        <activity android:name=".activities.pdf.PDFViewActivity" />
    </application>
</manifest>
